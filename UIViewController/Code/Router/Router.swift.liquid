{% include 'header' %}

import UIKit

final class {{ module_info.name }}Router: RouterType {
    
    // MARK: - External dependencies
    
    var controllerLoader: ControllerLoader! {
        didSet {
            baseController = controllerLoader.controller(by: {{ module_info.name }}ViewController.self)
        }
    }
    
    // MARK: - Public variables
    
    weak var baseController: UIViewController?
    
}
